// src/services/googleDriveService.ts

// Placeholder - we'll add Google Drive later if needed
export async function backupPhotoToDrive(
  photoId: string,
  blob: Blob
): Promise<void> {
  // Skipped for now
  console.log('Google Drive backup skipped');
}

